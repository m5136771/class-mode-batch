:: Git download page

:: Prompt, press enter when ready
:: Prompt, you have a GitHub account?
:: if no, open join page
:: if yes, open sign in page

:: prompt, already have a dedicated folder for coding projects?
:: if yes, prompt directory, proceed to next
:: if no, prompt, create now?
:: --if yes, create on desktop
:: --if no, skip

:: prompt, do you have a github webpage repo already?
:: if no, prompt username, create repo folder


:: prompt, press enter when ready
:: VS Code download page